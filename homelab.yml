---
- name: Configure host OS
  hosts: quadlet_hosts
  become: true
  vars:
    ansible_user: "{{ admin_user }}"
  roles:
    - quadlet_host

- name: Install Podman Quadlets
  hosts: quadlet_hosts
  become: true
  vars:
    ansible_user: "{{ admin_user }}"
  roles:
    - quadlets
