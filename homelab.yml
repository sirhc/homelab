---
- name: Configure host OS
  hosts: local
  become: true
  vars:
    ansible_user: "{{ admin_user }}"
  roles:
    - quadlet_host

- name: Install Podman Quadlets
  hosts: local
  become: true
  vars:
    ansible_user: "{{ admin_user }}"
  roles:
    - quadlets
