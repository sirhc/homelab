---
- name: System baseline
  hosts: all
  become: true
  vars:
    ansible_user: "{{ admin_user }}"
  roles:
    - system
    - restic

- name: Mail server
  hosts: outpost
  become: true
  vars:
    ansible_user: "{{ admin_user }}"
  roles:
    - mail
