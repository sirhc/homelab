---
- name: Locate drop-in template directories
  ansible.builtin.stat:
    path: "{{ role_path }}/templates/drop-in/{{ item }}"
  loop: "{{ enabled_services }}"
  register: drop_in_dirs
  delegate_to: localhost
  become: false

- name: Find drop-in files
  ansible.builtin.find:
    paths: "{{ item.stat.path }}"
    patterns: "*.j2"
    recurse: true
  loop: "{{ drop_in_dirs.results | selectattr('stat.exists') | selectattr('stat.isdir') | list }}"
  loop_control:
    label: "{{ item.item }}"
  register: drop_in_files
  delegate_to: localhost
  become: false

- name: Create drop-in directories
  ansible.builtin.file:
    path: "{{ homelab_quadlet_dir }}/{{ item }}.container.d"
    state: directory
    mode: "0755"
    owner: "{{ homelab_user }}"
    group: "{{ homelab_group }}"
  loop: >-
    {{ drop_in_files.results | selectattr('matched', 'gt', 0)
       | map(attribute='item.item') | list }}

- name: Deploy drop-in files
  vars:
    rel: "{{ item.path | relpath(role_path ~ '/templates/drop-in') }}"
  ansible.builtin.template:
    src: "{{ item.path }}"
    dest: >-
      {{ homelab_quadlet_dir }}/{{ rel.split('/')[0] }}.container.d/{{
         rel.split('/')[1:] | join('/') | regex_replace('\.j2$', '') }}
    mode: "0644"
    owner: "{{ homelab_user }}"
    group: "{{ homelab_group }}"
  loop: "{{ drop_in_files.results | map(attribute='files') | flatten }}"
  loop_control:
    label: "{{ item.path | relpath(role_path ~ '/templates/drop-in') }}"
  register: quadlet_dropin_files
  notify: Reload systemd user daemon
