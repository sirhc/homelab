---
# Each service directory holds its own .container and any .volume files, so the
# whole directory is copied verbatim into the Quadlet directory.
- name: Deploy Quadlet files
  ansible.builtin.copy:
    src: "services/{{ item }}/"
    dest: "{{ homelab_quadlet_dir }}/"
    mode: "0644"
    owner: "{{ homelab_user }}"
    group: "{{ homelab_group }}"
  loop: "{{ enabled_services }}"
  register: quadlet_unit_files
  notify: Reload systemd user daemon
