[Container]
# Labels every container for podman-auto-update(1) so `podman auto-update [--dry-run]`
# can act on them. Updates stay manual: the podman-auto-update.timer is masked by the
# quadlets role, so nothing runs on a schedule.
AutoUpdate=registry
