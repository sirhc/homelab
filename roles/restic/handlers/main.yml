---
- name: Reload user systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true
    scope: user
  become: true
  become_user: "{{ restic_user }}"
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ restic_uid }}"
    DBUS_SESSION_BUS_ADDRESS: "unix:path=/run/user/{{ restic_uid }}/bus"
